[pytest]
# Ensures the project root (where similarity.py, candidate_gen.py, etc.
# live) is on sys.path when pytest collects tests/, so `from similarity
# import SimilarityCalculator` resolves without a src-layout or install step.
pythonpath = .
testpaths = tests
